import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Skeleton } from "@/components/ui/skeleton";
import { trpc } from "@/lib/trpc";
import { CalendarDays, ChevronRight, Filter, FolderKanban, Plus, Search, SlidersHorizontal, X } from "lucide-react";
import { useMemo, useState } from "react";
import { useLocation } from "wouter";

const dateLabel = (value: Date | string | null | undefined) => value ? new Intl.DateTimeFormat("fr-FR", { dateStyle: "medium", timeZone: "UTC" }).format(new Date(value)) : "—";
const badgeStyle = (value?: string | null) => value === "Régularisé" ? "bg-[#e3f1ea] text-[#176b55]" : value === "Haute" || value === "À régulariser" ? "bg-[#fff0eb] text-[#bf5038]" : value === "Normale" ? "bg-[#fff5df] text-[#9e6a08]" : "bg-[#edf5f1] text-[#2d7664]";

function DossiersContent() {
  const [, setLocation] = useLocation();
  const [search, setSearch] = useState("");
  const [status, setStatus] = useState("");
  const [priority, setPriority] = useState("");
  const [client, setClient] = useState("");
  const [transportMode, setTransportMode] = useState("");
  const [etaFrom, setEtaFrom] = useState("");
  const [etaTo, setEtaTo] = useState("");
  const queryInput = useMemo(() => ({ search: search || undefined, status: (status || undefined) as "Régularisé" | "À régulariser" | undefined, priority: (priority || undefined) as "Haute" | "Normale" | "Basse" | undefined, client: client || undefined, transportMode: transportMode || undefined, etaFrom: etaFrom ? new Date(`${etaFrom}T00:00:00Z`) : undefined, etaTo: etaTo ? new Date(`${etaTo}T23:59:59Z`) : undefined }), [search, status, priority, client, transportMode, etaFrom, etaTo]);
  const { data: dossiers, isLoading, error } = trpc.dossier.list.useQuery(queryInput);
  const { data: refs } = trpc.reference.list.useQuery();
  const transportModes = refs?.filter(item => item.category === "mode_transport") ?? [];
  const clients = refs?.filter(item => item.category === "client") ?? [];
  const reset = () => { setSearch(""); setStatus(""); setPriority(""); setClient(""); setTransportMode(""); setEtaFrom(""); setEtaTo(""); };
  const activeFilters = [status, priority, client, transportMode, etaFrom, etaTo].filter(Boolean).length;
  return <div className="mx-auto max-w-[1540px] space-y-6">
    <div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#7b8e88]">Registre opérationnel</p><h1 className="mt-1 font-[Georgia] text-3xl font-semibold tracking-tight text-[#15372f]">Dossiers de transit</h1><p className="mt-2 text-sm text-[#71807b]">Recherche, qualification et suivi continu de l’ensemble des opérations.</p></div><Button onClick={() => setLocation("/dossiers/nouveau")} className="h-11 rounded-xl bg-[#0f4035] px-5 hover:bg-[#195847]"><Plus className="mr-2" size={17} />Nouveau dossier</Button></div>
    <Card className="border-0 bg-white shadow-[0_10px_28px_rgba(23,54,46,0.06)]"><CardContent className="p-4 sm:p-5"><div className="grid gap-3 lg:grid-cols-[minmax(240px,1.7fr)_repeat(3,minmax(135px,0.7fr))]"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-[#83938d]" size={17} /><Input value={search} onChange={event => setSearch(event.target.value)} placeholder="Rechercher numéro, client, BL / LTA…" className="h-10 rounded-xl border-[#e3ebe7] pl-10" /></div><select value={status} onChange={event => setStatus(event.target.value)} className="h-10 rounded-xl border border-[#e3ebe7] bg-white px-3 text-sm text-[#3f5851] focus:outline-none focus:ring-2 focus:ring-[#2f826d]/30"><option value="">Tous les statuts</option><option value="Régularisé">Régularisé</option><option value="À régulariser">À régulariser</option></select><select value={priority} onChange={event => setPriority(event.target.value)} className="h-10 rounded-xl border border-[#e3ebe7] bg-white px-3 text-sm text-[#3f5851] focus:outline-none focus:ring-2 focus:ring-[#2f826d]/30"><option value="">Toutes priorités</option><option value="Haute">Haute</option><option value="Normale">Normale</option><option value="Basse">Basse</option></select><select value={transportMode} onChange={event => setTransportMode(event.target.value)} className="h-10 rounded-xl border border-[#e3ebe7] bg-white px-3 text-sm text-[#3f5851] focus:outline-none focus:ring-2 focus:ring-[#2f826d]/30"><option value="">Tout transport</option>{transportModes.map(item => <option key={item.id} value={item.label}>{item.label}</option>)}</select></div><div className="mt-3 grid gap-3 border-t border-[#edf2ef] pt-3 lg:grid-cols-[minmax(240px,0.7fr)_1fr_auto]"><select value={client} onChange={event => setClient(event.target.value)} className="h-10 rounded-xl border border-[#e3ebe7] bg-white px-3 text-sm text-[#3f5851] focus:outline-none focus:ring-2 focus:ring-[#2f826d]/30"><option value="">Tous les clients</option>{clients.map(item => <option key={item.id} value={item.label}>{item.label}</option>)}</select><div className="flex flex-1 flex-wrap items-center gap-2 text-xs text-[#74847f]"><CalendarDays size={15} /><span className="font-medium">Plage ETA</span><Input type="date" value={etaFrom} onChange={event => setEtaFrom(event.target.value)} className="h-8 w-[145px] rounded-lg text-xs" /><span>au</span><Input type="date" value={etaTo} onChange={event => setEtaTo(event.target.value)} className="h-8 w-[145px] rounded-lg text-xs" /></div><div className="flex items-center gap-2"><Badge className="border-0 bg-[#e8f1ed] text-[#286c5a]"><Filter className="mr-1" size={12} />{activeFilters} filtre{activeFilters > 1 ? "s" : ""}</Badge>{Boolean(search || activeFilters) && <button onClick={reset} className="flex items-center gap-1 text-xs font-semibold text-[#8a4a38] hover:underline"><X size={13} />Réinitialiser</button>}</div></div></CardContent></Card>
    {error ? <Card className="border-0 bg-white"><CardContent className="p-10 text-center"><p className="font-semibold text-[#ad4c38]">Impossible de charger les dossiers</p><p className="mt-2 text-sm text-[#71817b]">{error.message}</p></CardContent></Card> : <Card className="overflow-hidden border-0 bg-white shadow-[0_10px_28px_rgba(23,54,46,0.06)]"><div className="flex items-center justify-between border-b border-[#edf2ef] px-5 py-4"><div className="flex items-center gap-2"><FolderKanban size={18} className="text-[#1d7764]" /><p className="font-semibold text-[#183c33]">{dossiers?.length ?? 0} dossier{(dossiers?.length ?? 0) > 1 ? "s" : ""}</p></div><div className="hidden items-center gap-1 text-xs text-[#82918c] md:flex"><SlidersHorizontal size={14} />Triés par dernière mise à jour</div></div><div className="overflow-x-auto"><table className="w-full min-w-[2200px] text-left"><thead className="bg-[#f8faf9]"><tr>{["N° dossier", "N° client", "Client", "BL / LTA", "Marchandise", "Transport", "ETA", "POL", "POD", "Conteneur", "Vrac", "Sortie", "Déclaration", "Bulletin", "Décl. définitive", "Statut", "Priorité", ""].map(label => <th key={label} className="px-4 py-3 text-[10px] font-bold uppercase tracking-[0.12em] text-[#7b8c86]">{label}</th>)}</tr></thead><tbody className="divide-y divide-[#eff3f1]">{isLoading ? Array.from({ length: 8 }).map((_, index) => <tr key={index}>{Array.from({ length: 18 }).map((__, cell) => <td key={cell} className="px-4 py-3"><Skeleton className="h-5 w-full rounded" /></td>)}</tr>) : dossiers?.map(dossier => <tr key={dossier.id} className="group transition hover:bg-[#f8fbf9]"><td className="px-4 py-3"><button onClick={() => setLocation(`/dossiers/${dossier.id}`)} className="font-semibold text-[#176b55] hover:underline">{dossier.dossierNumber}</button></td><td className="px-4 py-3 text-sm text-[#536863]">{dossier.clientDossierNumber || "—"}</td><td className="max-w-[180px] px-4 py-3"><p className="truncate text-sm font-medium text-[#27463e]">{dossier.client || "—"}</p></td><td className="px-4 py-3 text-sm text-[#536863]">{dossier.blLtaNumber || "—"}</td><td className="max-w-[220px] px-4 py-3"><p className="truncate text-sm text-[#536863]">{dossier.cargoNature || "—"}</p></td><td className="px-4 py-3 text-sm text-[#536863]">{dossier.transportMode || "—"}</td><td className="px-4 py-3 text-sm text-[#536863]">{dateLabel(dossier.eta)}</td><td className="max-w-[150px] px-4 py-3 text-sm text-[#536863]"><p className="truncate">{dossier.originPort || "—"}</p></td><td className="max-w-[160px] px-4 py-3 text-sm text-[#536863]"><p className="truncate">{dossier.destinationPort || "—"}</p></td><td className="px-4 py-3 text-sm text-[#536863]">{dossier.container || "—"}</td><td className="px-4 py-3 text-sm text-[#536863]">{dossier.bulk || "—"}</td><td className="px-4 py-3 text-sm text-[#536863]">{dateLabel(dossier.goodsReleaseDate)}</td><td className="px-4 py-3 text-sm text-[#536863]">{dossier.declarationNumber || "—"}</td><td className="px-4 py-3 text-sm text-[#536863]">{dossier.bulletinNumber || "—"}</td><td className="px-4 py-3 text-sm text-[#536863]">{dossier.finalDeclarationNumber || "—"}</td><td className="px-4 py-3"><Badge className={`whitespace-nowrap border-0 ${badgeStyle(dossier.calculatedStatus)}`}>{dossier.calculatedStatus}</Badge></td><td className="px-4 py-3"><Badge className={`border-0 ${badgeStyle(dossier.calculatedPriority)}`}>{dossier.calculatedPriority}</Badge></td><td className="px-4 py-3"><button aria-label={`Ouvrir ${dossier.dossierNumber}`} onClick={() => setLocation(`/dossiers/${dossier.id}`)} className="grid h-8 w-8 place-items-center rounded-lg text-[#789088] transition hover:bg-[#e6f0eb] hover:text-[#176b55]"><ChevronRight size={17} /></button></td></tr>)}</tbody></table></div>{!isLoading && dossiers?.length === 0 && <div className="grid min-h-56 place-items-center p-8 text-center"><div><Search className="mx-auto mb-3 text-[#8da099]" size={28} /><p className="font-semibold text-[#28483f]">Aucun dossier ne correspond aux critères.</p><button onClick={reset} className="mt-2 text-sm font-semibold text-[#1d7764] hover:underline">Effacer les filtres</button></div></div>}</Card>}
  </div>;
}
export default function DossiersPage() { return <DashboardLayout><DossiersContent /></DashboardLayout>; }
